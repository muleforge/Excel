<!--
Assembly descriptor for the full distribution.
-->
<assembly>
    <id>EXCEL</id>
    <formats>
        <format>zip</format>
    </formats>
    <fileSets>
        <fileSet>
            <directory>src</directory>
            <outputDirectory>src</outputDirectory>
        </fileSet>
        <fileSet>
            <directory>../target</directory>
            <outputDirectory>.</outputDirectory>
            <includes>
                <include>*.jar</include>
            </includes>
        </fileSet>
    </fileSets>
    <dependencySets>
        <dependencySet>
            <outputDirectory>lib/user</outputDirectory>
            <scope>runtime</scope>
            <!-- Default Mule excludes -->
            <excludes>
                <!-- don't pull in any Mule jars since these should be provided -->
                <exclude>org.mule:*</exclude>
                <!-- Mule prefers SLF4J -->
                <exclude>commons-logging:commons-logging</exclude>
            </excludes>

            <!-- Include only the non-Mule libraries needed by your application -->
            <includes>
                <!-- Make sure we include this project's artifact -->
                <include>${groupId}:${artifactId}</include>
                <!-- JXLS Libraray -->
                <include>net.sf.jxls:*</include>
            </includes>

        </dependencySet>
    </dependencySets>
</assembly>
